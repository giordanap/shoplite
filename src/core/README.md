# Core

The `core` layer contains frontend infrastructure that is not owned by a specific business feature.

Examples:

- API clients
- HTTP helpers
- Query configuration
- Storage helpers
- Router helpers
- Pagination utilities
- Shared error types

This layer should not render business UI.