# Shared

The `shared` layer contains reusable pieces that are not specific to one feature.

Examples:

- UI primitives
- Layout components
- Feedback states
- Skeletons
- Shared hooks
- Constants
- Generic utilities

Shared code should stay generic and avoid business-specific logic.